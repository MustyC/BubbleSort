package com.sparta.mc.alogrithms;

public class BinarySearchTree {
  public static void binaryTreeSort(int[] unsortedArray ) {
//  Take an array as an input
//  Create a BST by inserting data values from the array into the BST
//  Preform in-order traversal on the tree to get the elements in sorted order
    // to do this, go left --> root --> right recursively to sort the array

    int parent = 0;
    int leftChild = 0;
    int rightChild = 0;

    for ( int i = 0; i < unsortedArray.length; i++ ) {
      if(){

      } else if (){

      }
    }
  }
}
